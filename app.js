document.addEventListener('alpine:init', () => { Alpine.data('vaultApp', () => ({ folders: [], searchQuery: '', isAddFolderOpen: false, newFolderName: '', confirmDeleteFolderData: null, isWipeAllOpen: false, moveModelData: null, selectedLinkIds: [], selectedFolderIds: [], isBulkMoveModalOpen: false, isFolderBulkMoveModalOpen: false, isMergeModalOpen: false, toasts: [], init() { const saved = localStorage.getItem('meroVaultData'); if (saved) { try { this.folders = JSON.parse(saved); } catch (e) { this.folders = []; } } }, saveData() { localStorage.setItem('meroVaultData', JSON.stringify(this.folders)); }, get filteredActiveLinks() { if (!this.searchQuery) return []; const q = this.searchQuery.toLowerCase(); let results = []; this.folders.forEach(f => { if (f.links) { f.links.forEach(l => { if (l.title.toLowerCase().includes(q) || l.url.toLowerCase().includes(q)) { results.push(l); } }); } }); return results; } })); });
